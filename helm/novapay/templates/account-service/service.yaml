apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: account-service
  ports:
    - port: {{ .Values.services.account.port }}
      targetPort: {{ .Values.services.account.port }}
